# Unraid Templates for wp-final-porttest

XML templates for Unraid Community Applications.

## Install
Copy `my-wp-final-porttest.xml` to `/boot/config/plugins/dockerMan/templates-user/` on Unraid,
then: Docker tab → Add Container → select from template list.
